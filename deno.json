{
  "name": "@mewhhaha/itp",
  "version": "0.1.0",
  "license": "MIT",
  "exports": {
    ".": "./mod.ts"
  },
  "publish": {
    "include": [
      "README.md",
      "CHANGELOG.md",
      "LICENSE",
      "deno.json",
      "mod.ts",
      "docs/**/*.md",
      "examples/**/*.json",
      "examples/**/*.md",
      "examples/**/*.ts",
      "src/**/*.ts"
    ],
    "exclude": [
      "src/assert.ts",
      "src/*.test.ts"
    ]
  },
  "tasks": {
    "ci": "deno task fmt:check && deno task lint && deno task doc:lint && deno task check && deno task test && deno task examples && deno task publish:dry-run",
    "check": "deno check mod.ts src/*.test.ts examples/*.ts",
    "doc:lint": "deno doc --lint mod.ts",
    "examples": "deno run examples/pricing_dsl.ts && deno run examples/functions_dsl.ts && deno run examples/rules_engine.ts && deno run examples/json_key_metrics.ts",
    "fmt": "deno fmt",
    "fmt:check": "deno fmt --check",
    "lint": "deno lint",
    "publish:dry-run": "deno publish --dry-run --allow-dirty",
    "test": "deno test"
  }
}
